#include<iostream>
#include<string>
using namespace std;

int main() {
	int n;
	int sum = 0;
	string s;
	cin >> n >> s;
	for (int i = 0; i < s.size(); i++) {
		sum += stoi(s.substr(i, 1));
	}
	cout << sum;
	
	return 0;
}
